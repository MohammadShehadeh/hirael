import { Inter, Space_Grotesk } from "next/font/google"

export const inter = Inter({
  variable: "--font-rivr-sans",
  subsets: ["latin"],
  display: "swap",
})

export const spaceGrotesk = Space_Grotesk({
  variable: "--font-rivr-display",
  subsets: ["latin"],
  display: "swap",
})
