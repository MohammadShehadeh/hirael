# Ignore artifacts:
build
coverage
.next
node_modules
out

# Generated by scripts/* (registry:gen / registry:props) — these must match
# the generators' byte-for-byte output, which check:registry enforces. Prettier
# reformats them (e.g. collapsing JSON arrays) and breaks that check.
registry.json
vercel.json
registry/hirael/registry-props.json
